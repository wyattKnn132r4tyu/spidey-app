<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive icon: the launcher masks the foreground, so the face is inset and
     the navy plate carries the background. -->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/ink2" />
    <foreground android:drawable="@mipmap/ic_launcher_foreground" />
</adaptive-icon>
